{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "$id": "my-step",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Library name",
      "$default": {
        "$source": "argv",
        "index": 0
      }
    },
    "project": {
      "type": "string",
      "description": "Target project"
    }
  },
  "required": ["name"]
}
